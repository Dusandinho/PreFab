[pytest]
testpaths = fabmodel tests
# addopts = --tb=no
addopts = '--tb=short'
python_files = fabmodel/*.py tests/*.py

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning

# addopts = '--html=test_report.html' '--self-contained-html'
